// Empty component to prevent import errors
// Scroll animations have been removed to fix deployment issues

function ScrollAnimationsInit() {
  // This component doesn't do anything
  return null;
}

export default ScrollAnimationsInit;
